{% include '_partials/action_switch/toggle_view_edit_top.html.twig' %}

<dl class="row rbins-main-tab-content">
    <dt class="col-xs-4 col-md-3 col-lg-2" title="Language">
        Language&nbsp;:
    </dt>
    <dd class="col-xs-8 col-md-9 col-lg-10">
        {{ item.internationalNameLanguage }}
    </dd>
    {% if item.internationalDescription is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="International description">
            Description (Int.)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 col-lg-10">
            {{ item.internationalDescription }}
        </dd>
    {% endif %}
    {% if item.nameEn is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Name in english">
            Name (EN)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.descriptionEn is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.nameEn }}
        </dd>
    {% endif %}
    {% if item.descriptionEn is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Description in english">
            Description (EN)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.nameEn is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.descriptionEn }}
        </dd>
    {% endif %}
    {% if item.nameNl is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Name in dutch">
            Name (NL)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.descriptionNl is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.nameNl }}
        </dd>
    {% endif %}
    {% if item.descriptionNl is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Description in dutch">
            Description (NL)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.nameNl is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.descriptionNl }}
        </dd>
    {% endif %}
    {% if item.nameFr is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Name in french">
            Name (FR)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.descriptionFr is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.nameFr }}
        </dd>
    {% endif %}
    {% if item.descriptionFr is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Description in french">
            Description (FR)&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.nameFr is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.descriptionFr }}
        </dd>
    {% endif %}
    {% if item.startDate is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Project start date">
            Start date&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.endDate is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.startDate|date('d/m/Y') }}
        </dd>
    {% endif %}
    {% if item.endDate is not empty %}
        <dt class="col-xs-4 col-md-3 col-lg-2" title="Project end date">
            End date&nbsp;:
        </dt>
        <dd class="col-xs-8 col-md-9 {% if item.startDate is not empty %}col-lg-4{% else %}col-lg-10{% endif %}">
            {{ item.endDate|date('d/m/Y') }}
        </dd>
    {% endif %}
</dl>
